apiVersion: v1
kind: Service
metadata:
  name: app2-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
  selector:
    app: app2



# 서로 같은 ip type은 사용이 안됨
# port 번호 app1 이랑 같게하기